// Q.8:- Find last element of an array ?
// ( Withour using Length property )

const arr = ["White", "Black", "red", "yellow"];
console.log("??????????");

// The at() method takes an integer value.
// Returns the item at that index
// allow positive & Negative integer :- (-1)

console.log(arr.at(-1));
arr.push("Gray;");

console.log(arr.at(-1));
