// GIven array Find the Output of the Code ?
// 1.
const strFrom = Array.from("testform");
console.log(strFrom);
// 2.
const arrFrom = Array.from([1, 2, 3], (x) => x + x);
console.log(arrFrom);
// 3.
const result = Array.from({ length: 10 }, (a, b) => b);
console.log(result);
